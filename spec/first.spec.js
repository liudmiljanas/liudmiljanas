//require('../src/mylib.js');

describe("my suite", function() {
	it("should do stuff", function() {
		expect(1).toBe(1);
	});
	it("should do stuff2", function() {
		init();		
		expect(1).toBe(1);
	});

});
